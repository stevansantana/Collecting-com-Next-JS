import { Navbar } from "../Navbar/Navbar"

export const Header:React.FC = () => {

    return(
        <header className="p-8 bg-white shadow-md">
           <Navbar></Navbar>
        </header>
    ) 
}